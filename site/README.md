# OpenKP website

Static landing page for `openkp.org`.

Cloudflare Pages settings:

- Build command: none
- Build output directory: `site`
- Custom domain: `openkp.org`

The site intentionally has no runtime dependencies and no analytics.
